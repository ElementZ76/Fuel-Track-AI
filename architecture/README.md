# Architecture Layer
This directory contains Standard Operating Procedures (SOPs) written in Markdown.
Each SOP defines goals, inputs, tool logic, and edge cases for a specific workflow.

**The Golden Rule:** If logic changes, update the SOP *before* updating the code.
