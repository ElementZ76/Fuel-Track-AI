# Tools Layer
This directory contains deterministic Python scripts — the execution engines.
Each tool is atomic, testable, and free of probabilistic logic.

Environment variables and tokens are stored in `.env` at the project root.
